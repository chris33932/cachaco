<form method="post" action="{{ path('nivel_inst_formal_delete', {'id': nivel_inst_formal.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ nivel_inst_formal.id) }}">
    <button class="btn">Delete</button>
</form>
