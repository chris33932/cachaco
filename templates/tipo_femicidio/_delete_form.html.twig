<form method="post" action="{{ path('tipo_femicidio_delete', {'id': tipo_femicidio.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tipo_femicidio.id) }}">
    <button class="btn">Delete</button>
</form>
