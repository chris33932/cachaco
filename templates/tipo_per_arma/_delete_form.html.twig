<form method="post" action="{{ path('tipo_per_arma_delete', {'id': tipo_per_arma.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tipo_per_arma.id) }}">
    <button class="btn">Delete</button>
</form>
