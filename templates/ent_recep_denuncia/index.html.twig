{% extends 'base.html.twig' %}

{% block title %}EntRecepDenuncia index{% endblock %}

{% block body %}
    <h1>EntRecepDenuncia index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Descripcion</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for ent_recep_denuncium in ent_recep_denuncias %}
            <tr>
                <td>{{ ent_recep_denuncium.id }}</td>
                <td>{{ ent_recep_denuncium.descripcion }}</td>
                <td>
                    <a href="{{ path('ent_recep_denuncia_show', {'id': ent_recep_denuncium.id}) }}">show</a>
                    <a href="{{ path('ent_recep_denuncia_edit', {'id': ent_recep_denuncium.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('ent_recep_denuncia_new') }}">Create new</a>
{% endblock %}
