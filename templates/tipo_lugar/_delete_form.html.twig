<form method="post" action="{{ path('tipo_lugar_delete', {'id': tipo_lugar.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tipo_lugar.id) }}">
    <button class="btn">Delete</button>
</form>
