<form method="post" action="{{ path('sit_procesal_delete', {'id': sit_procesal.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sit_procesal.id) }}">
    <button class="btn">Delete</button>
</form>
