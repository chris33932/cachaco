<form method="post" action="{{ path('pres_autor_delete', {'id': pres_autor.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pres_autor.id) }}">
    {#<button class="btn">Delete</button>#}
</form>
