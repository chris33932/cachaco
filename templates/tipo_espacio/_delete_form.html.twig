<form method="post" action="{{ path('tipo_espacio_delete', {'id': tipo_espacio.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tipo_espacio.id) }}">
    <button class="btn">Delete</button>
</form>
