{% extends 'base.html.twig' %}

{% block title %}TipoEspacio{% endblock %}

{% block body %}
    <h1>TipoEspacio</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tipo_espacio.id }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ tipo_espacio.descripcion }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('tipo_espacio_index') }}">back to list</a>

    <a href="{{ path('tipo_espacio_edit', {'id': tipo_espacio.id}) }}">edit</a>

    {{ include('tipo_espacio/_delete_form.html.twig') }}
{% endblock %}
