<form method="post" action="{{ path('condicion_actividad_delete', {'id': condicion_actividad.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ condicion_actividad.id) }}">
    <button class="btn">Delete</button>
</form>
