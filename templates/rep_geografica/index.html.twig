{% extends 'base.html.twig' %}

{% block title %}RepGeografica index{% endblock %}

{% block body %}
    <h1>RepGeografica index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Descripcion</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for rep_geografica in rep_geograficas %}
            <tr>
                <td>{{ rep_geografica.id }}</td>
                <td>{{ rep_geografica.descripcion }}</td>
                <td>
                    <a href="{{ path('rep_geografica_show', {'id': rep_geografica.id}) }}">show</a>
                    <a href="{{ path('rep_geografica_edit', {'id': rep_geografica.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('rep_geografica_new') }}">Create new</a>
{% endblock %}
