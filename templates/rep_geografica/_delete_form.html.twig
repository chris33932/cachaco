<form method="post" action="{{ path('rep_geografica_delete', {'id': rep_geografica.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ rep_geografica.id) }}">
    <button class="btn">Delete</button>
</form>
