<form method="post" action="{{ path('mecanismo_muerte_delete', {'id': mecanismo_muerte.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mecanismo_muerte.id) }}">
    <button class="btn">Delete</button>
</form>
