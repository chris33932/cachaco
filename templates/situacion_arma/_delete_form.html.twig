<form method="post" action="{{ path('situacion_arma_delete', {'id': situacion_arma.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ situacion_arma.id) }}">
    <button class="btn">Delete</button>
</form>
