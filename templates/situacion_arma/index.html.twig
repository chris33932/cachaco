{% extends 'base.html.twig' %}

{% block title %}SituacionArma index{% endblock %}

{% block body %}
    <h1>SituacionArma index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Descripcion</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for situacion_arma in situacion_armas %}
            <tr>
                <td>{{ situacion_arma.id }}</td>
                <td>{{ situacion_arma.descripcion }}</td>
                <td>
                    <a href="{{ path('situacion_arma_show', {'id': situacion_arma.id}) }}">show</a>
                    <a href="{{ path('situacion_arma_edit', {'id': situacion_arma.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('situacion_arma_new') }}">Create new</a>
{% endblock %}
