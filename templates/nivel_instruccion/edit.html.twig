{% extends 'base.html.twig' %}

{% block title %}Edit NivelInstruccion{% endblock %}

{% block body %}
    <h1>Edit NivelInstruccion</h1>

    {{ include('nivel_instruccion/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('nivel_instruccion_index') }}">back to list</a>

    {{ include('nivel_instruccion/_delete_form.html.twig') }}
{% endblock %}
