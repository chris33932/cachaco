{% extends 'base.html.twig' %}

{% block title %}NivelInstruccion{% endblock %}

{% block body %}
    <h1>NivelInstruccion</h1>
        <div class="container">

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Descripcion</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for nivel_instruccion in nivel_instruccions %}
            <tr>
                <td>{{ nivel_instruccion.id }}</td>
                <td>{{ nivel_instruccion.descripcion }}</td>
                <td>
                    <a href="{{ path('nivel_instruccion_show', {'id': nivel_instruccion.id}) }}">show</a>
                    <a href="{{ path('nivel_instruccion_edit', {'id': nivel_instruccion.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('nivel_instruccion_new') }}">Create new</a>
{% endblock %}
